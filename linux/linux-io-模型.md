---
type: knowledge
status: evergreen
created: 2026-06-13
updated: 2026-06-13
related_to:
  - "[[Linux 系统原理]]"
  - "[[dns-cdn-http-https]]"
has:
  - "[[select]]"
  - "[[poll]]"
  - "[[epoll]]"
  - "[[红黑树]]"
  - "[[Page Cache]]"
_organized: true
---

# Linux I/O 模型

## 基本概念

Linux 程序读写文件或网络连接时，通过系统调用进入内核：

```text
用户进程 → 系统调用 → 内核 → 文件系统/网络协议栈 → 硬件
```

| 概念 | 说明 |
| --- | --- |
| 文件描述符 | 进程访问文件、socket、管道等资源的编号 |
| 用户态 | 应用程序运行空间 |
| 内核态 | 内核运行空间 |
| Page Cache | 内核用于缓存文件数据的内存区域 |
| 阻塞 I/O | 调用后等待数据就绪 |
| 非阻塞 I/O | 没有数据时立即返回 |
| I/O 多路复用 | 一个进程同时监听多个文件描述符 |

## select、poll、epoll

### select

| 问题 | 说明 |
| --- | --- |
| 文件描述符数量限制 | 通常受 `FD_SETSIZE` 限制 |
| 每次都要拷贝集合 | 用户态和内核态之间传递成本高 |
| 每次线性扫描 | 连接多时效率下降 |

### poll

改进了 select 的文件描述符数量限制，但仍然需要线性扫描。

### epoll

Linux 高并发网络服务常用机制：

| 特点 | 说明 |
| --- | --- |
| 不需要每次传入完整 fd 集合 | 先注册，再等待事件 |
| 事件驱动 | 只返回就绪事件 |
| 更适合大量连接 | 高并发服务器常用 |
| 支持水平触发和边缘触发 | LT/ET 模式 |

典型系统调用：`epoll_create` → `epoll_ctl` → `epoll_wait`。

Nginx、Redis 等高并发程序常依赖 I/O 多路复用模型。

## 红黑树

自平衡二叉搜索树，常见操作复杂度 `O(log n)`。Linux 内核中很多结构使用红黑树管理有序对象。

## 易错点

| 问题 | 说明 |
| --- | --- |
| `select/poll/epoll` 混为一谈 | 三者都是 I/O 多路复用，但性能模型不同 |
